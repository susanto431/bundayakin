# BundaYakin — Monorepo

Platform kecocokan dan pemantauan nanny berbasis psikologi.

## Apps

- `apps/web` — Next.js 14 App Router → Vercel
- `apps/pdf-service` — Python ReportLab PDF generator → Railway

## Setup

```bash
cd apps/web && npm install && npm run dev
```
